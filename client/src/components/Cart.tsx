import React from 'react';
import {Container} from '@mui/material';

function Cart(): React.ReactElement<void> {
  return (
      <Container>
        <h1>child</h1>
      </Container>
  );
}

export default Cart;
